(slice, int) unpack_user_asset_principal_item(slice user_asset_principal_item) {
    return (
		user_asset_principal_item~load_msg_addr(), ;; What is this?
		user_asset_principal_item~load_uint(64) ;; TODO:sepezho: what is this?
	);
}
